/**
 * Users events
 */
enum Event {
  UserCreate = 'event.users.user.create',
  UserUpdate = 'event.users.user.update',
  UserRemove = 'event.users.user.remove',
  UserRestore = 'event.users.user.restore',
}

export default Event;
